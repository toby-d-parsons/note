<div class="sidebar">
    <div>
        <%= link_to "+", new_page_path %>
    </div>
    <% @pages.each do |page| %>
      <div>
        <%= link_to page.title, edit_page_path(page.id) %>
      </div>
    <% end %>
</div>